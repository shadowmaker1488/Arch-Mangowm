# More option see https://github.com/DreamMaoMao/mango/wiki/
# ==========================================
# MANGOWM CONFIGURATION (Odpovídá Hyprlandu)
# ==========================================

# ------------------------------------------
# Nastavení monitorů
# ------------------------------------------
# mangowm si monitory primárně načítá z wlr automaticky, 
# ale zachováváme blok dle tvé struktury.

# ------------------------------------------
# Autostart
# ------------------------------------------
exec-once = waybar
exec-once = hypridle
exec-once = deluged
exec-once = awww-daemon
exec-once = swaync
exec-once = ~/.config/hypr/scripts/xdg-portal-hyprland
exec-once = lxqt-policykit-agent
exec-once = ~/.config/hypr/scripts/wlsunset.sh
exec-once = udiskie
exec-once = playerctld daemon
exec-once = clipse -listen
# Spuštění specifických aplikací (původně pro special workspaces v Hyprlandu)
exec-once = foot -e ncspot
exec-once = foot -e htop

# ------------------------------------------
# Env. variables
# ------------------------------------------
# (V mangowm se proměnné XDG a QT nastavují typicky ve spouštěcím wrapperu)
cursor_size=24

# ------------------------------------------
# Input (Klávesnice, Touchpad, Myš)
# ------------------------------------------
xkb_rules_layout=cz
numlockon=1
sloppyfocus=1

# Touchpad
tap_to_click=1
trackpad_natural_scrolling=0
disable_while_typing=0

# Mouse
mouse_natural_scrolling=0

# ------------------------------------------
# General (Gaps a Borders)
# ------------------------------------------
gappiv=2
gappih=2
gappov=2
gappoh=2
borderpx=2

# Barvy
focuscolor=0x9966ffff
bordercolor=0x595959aa

# ------------------------------------------
# Scrolling (Scroller layout)
# ------------------------------------------
scroller_default_proportion=0.5

# ------------------------------------------
# Master (Master-Stack layout)
# ------------------------------------------
new_is_master=1

# ------------------------------------------
# Misc
# ------------------------------------------
# Animace manuální změny velikosti atd. řeší mangowm přes layer/animations níže.

# ------------------------------------------
# Ecosystem
# ------------------------------------------
# (Žádné updaty v mangowm k vypnutí nejsou)

# ------------------------------------------
# Decoration
# ------------------------------------------
border_radius=10
blur=0
blur_params_radius=3
blur_params_num_passes=1

# ------------------------------------------
# Animations
# ------------------------------------------
animations=1
layer_animations=1

# ------------------------------------------
# Gestures
# ------------------------------------------
swipe_min_threshold=1

# ------------------------------------------
# Per-device config
# ------------------------------------------
# (epic-mouse-v1 sensitivity nelze přímo zapsat do tohoto ini)

# ------------------------------------------
# Window Rules
# ------------------------------------------
windowrule=width:560,height:570,appid:waypaper
windowrule=width:560,height:570,appid:clipse-gui
windowrule=width:560,height:570,appid:localsend
windowrule=width:1000,height:500,appid:lxqt-policykit-agent

# ------------------------------------------
# Variables / Key Bindings
# ------------------------------------------

# ZÁKLADNÍ OVLÁDÁNÍ OKEN
bind=SUPER,q,killclient
bind=SUPER,space,togglefloating,
bind=SUPER+SHIFT,s,switch_layout
bind=SUPER,f,togglefullscreen,
bind=SUPER,Delete,spawn,killall mangowm

# MEDIA & VOLUME (wpctl & playerctl)
bind=NONE,XF86AudioRaiseVolume,spawn,wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bind=NONE,XF86AudioLowerVolume,spawn,wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bind=NONE,XF86AudioMute,spawn,wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bind=NONE,XF86AudioPlay,spawn,playerctl play-pause
bind=NONE,XF86AudioPause,spawn,playerctl play-pause
bind=NONE,XF86AudioNext,spawn,playerctl next
bind=NONE,XF86AudioPrev,spawn,playerctl previous
bind=NONE,XF86AudioStop,spawn,playerctl stop
bind=NONE,XF86AudioMicMute,spawn,pactl set-source-mute 0 toggle

# JAS
bind=NONE,XF86MonBrightnessUp,spawn,brightnessctl set +5%
bind=NONE,XF86MonBrightnessDown,spawn,brightnessctl set 5%-

# APLIKACE
bind=SUPER+SHIFT,r,spawn,~/.config/waybar/scripts/reload.sh
bind=SUPER,d,spawn,foot -e yazi
bind=SUPER+SHIFT,m,spawn,foot -e pulsemixer
bind=SUPER,Return,spawn,foot
bind=SUPER,b,spawn,firefox
bind=CTRL+SHIFT,p,spawn,firefox --private-window
bind=SUPER+SHIFT,p,spawn,hyprpicker -a
bind=SUPER+ALT,l,spawn,hyprlock
bind=SUPER,c,spawn,foot -e calcurse
bind=SUPER,o,spawn,libreoffice
bind=SUPER,m,spawn,thunderbird
bind=SUPER,n,spawn,foot -e newsboat
bind=SUPER,w,spawn,waypaper
bind=SUPER,v,spawn,clipse-gui
bind=SUPER,p,spawn,localsend

# SCRATCHPAD (Adaptace special workspaces pro mangowm scratchpad)
bind=SUPER+SHIFT,z,minimized,
bind=SUPER,z,toggle_scratchpad,
bind=SUPER,s,toggle_scratchpad,
bind=SUPER,t,toggle_scratchpad,

# PRINTSCREEN
bind=NONE,Print,spawn,grimblast --notify --freeze copysave screen
bind=SHIFT,Print,spawn,grimblast copysave area --freeze --notify

# ROFI CONTROLS
bind=SUPER+SHIFT,f,spawn,rofi -show drun
bind=SUPER+SHIFT,c,spawn,~/.config/rofi/scripts/rofi-calc.sh
bind=SUPER+SHIFT,w,spawn,~/.config/waybar/scripts/waybar-theme.sh
bind=SUPER,Escape,spawn,~/.config/rofi/scripts/rofi-powermenu.sh

# MOVE FOCUS (Vim keys)
bind=SUPER,h,focusdir,left
bind=SUPER,l,focusdir,right
bind=SUPER,k,focusdir,up
bind=SUPER,j,focusdir,down

# RESIZE WINDOWS (Vim keys s CTRL)
bind=SUPER+CTRL,h,resizewin,-50,+0
bind=SUPER+CTRL,l,resizewin,+50,+0
bind=SUPER+CTRL,k,resizewin,+0,-50
bind=SUPER+CTRL,j,resizewin,+0,+50

# MOVE FOCUS (Šipky)
bind=SUPER,Left,focusdir,left
bind=SUPER,Right,focusdir,right
bind=SUPER,Up,focusdir,up
bind=SUPER,Down,focusdir,down

# RESIZE WINDOWS (Šipky s CTRL)
bind=SUPER+CTRL,Left,resizewin,-50,+0
bind=SUPER+CTRL,Right,resizewin,+50,+0
bind=SUPER+CTRL,Up,resizewin,+0,-50
bind=SUPER+CTRL,Down,resizewin,+0,+50

# MOVE WINDOWS (Přesouvání - Shift + Vim)
bind=SUPER+SHIFT,h,exchange_client,left
bind=SUPER+SHIFT,l,exchange_client,right
bind=SUPER+SHIFT,k,exchange_client,up
bind=SUPER+SHIFT,j,exchange_client,down

# PŘEPÍNÁNÍ WORKSPACES (Česká klávesnice horndí řada: mod + klávesa)
bind=SUPER,plus,view,1,0
bind=SUPER,ecaron,view,2,0
bind=SUPER,scaron,view,3,0
bind=SUPER,ccaron,view,4,0
bind=SUPER,rcaron,view,5,0
bind=SUPER,zcaron,view,6,0
bind=SUPER,yacute,view,7,0
bind=SUPER,aacute,view,8,0
bind=SUPER,iacute,view,9,0
bind=SUPER,eacute,view,10,0

# PŘESUNUTÍ OKNA NA WORKSPACE (Česká klávesnice horndí řada: mod + shift + klávesa)
bind=SUPER+SHIFT,plus,tag,1,0
bind=SUPER+SHIFT,ecaron,tag,2,0
bind=SUPER+SHIFT,scaron,tag,3,0
bind=SUPER+SHIFT,ccaron,tag,4,0
bind=SUPER+SHIFT,rcaron,tag,5,0
bind=SUPER+SHIFT,zcaron,tag,6,0
bind=SUPER+SHIFT,yacute,tag,7,0
bind=SUPER+SHIFT,aacute,tag,8,0
bind=SUPER+SHIFT,iacute,tag,9,0
bind=SUPER+SHIFT,eacute,tag,10,0

# PŘEPÍNÁNÍ WORKSPACES (Numpad)
bind=SUPER,KP_End,view,1,0
bind=SUPER,KP_Down,view,2,0
bind=SUPER,KP_Next,view,3,0
bind=SUPER,KP_Left,view,4,0
bind=SUPER,KP_Begin,view,5,0
bind=SUPER,KP_Right,view,6,0
bind=SUPER,KP_Home,view,7,0
bind=SUPER,KP_Up,view,8,0
bind=SUPER,KP_Prior,view,9,0
bind=SUPER,KP_Insert,view,10,0

# PŘESUNUTÍ OKNA NA WORKSPACE (Numpad + Shift)
bind=SUPER+SHIFT,KP_End,tag,1,0
bind=SUPER+SHIFT,KP_Down,tag,2,0
bind=SUPER+SHIFT,KP_Next,tag,3,0
bind=SUPER+SHIFT,KP_Left,tag,4,0
bind=SUPER+SHIFT,KP_Begin,tag,5,0
bind=SUPER+SHIFT,KP_Right,tag,6,0
bind=SUPER+SHIFT,KP_Home,tag,7,0
bind=SUPER+SHIFT,KP_Up,tag,8,0
bind=SUPER+SHIFT,KP_Prior,tag,9,0
bind=SUPER+SHIFT,KP_Insert,tag,10,0

# SCROLLOVÁNÍ WORKSPACES (mod + alt + šipky)
bind=SUPER+ALT,Right,viewtoright,0
bind=SUPER+ALT,Left,viewtoleft,0

# MYŠ (Mod + tlačítka)
mousebind=SUPER,btn_left,moveresize,curmove
mousebind=SUPER,btn_right,moveresize,curresize
