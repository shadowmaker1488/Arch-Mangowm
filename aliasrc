#/bin/bash

# BASIC
alias rm="rm -rfI"
alias df="dysk"
alias ls='eza -lh --group-directories-first --icons=auto'
alias ll="eza -la --icons"
alias cp="cp -ri"
alias cat="bat"
alias h="history | grep"

#alias df="df -h -x tmpfs"
alias f="fzf"
alias n="nvim"
alias batteryinfo="upower -i /org/freedesktop/UPower/devices/battery_BAT1"

# APPS
alias yt="yt-dlp -f bestaudio+bestvideo --remux-video mp4"
alias yta="yt-dlp -x --audio-format mp3 --write-thumbnail --convert-thumbnails jpg"
alias yt-album="yt-dlp -x --audio-format mp3 --split-chapters --audio-quality 0 --write-thumbnail --convert-thumbnails jpg"

alias calc="rofi -show calc"
alias y="yazi"

alias sl="songrec listen"

# wf-recorder 
alias record="wf-recorder -a=alsa_output.pci-0000_00_1f.3.analog-stereo.monitor --geometry 1920x1080 -f"
alias record-area='wf-recorder -g "$(slurp)" -a=alsa_output.pci-0000_00_1f.3.analog-stereo.monitor -f'
alias record-area-mic='wf-recorder -g "$(slurp)" -a=alsa_output.pci-0000_00_1f.3.analog-stereo.monitor --audio -f'
alias record-mic="wf-recorder -a analog-input-internal-mic --geometry 1920x1080 -f"

# CONFIG FILES 
alias nw="nvim ~/.config/waybar/config"
alias nva="nvim ~/.config/aliasrc"

# hyprland config files 
alias hc="nvim ~/.config/hypr/hyprland.lua"
alias hi="nvim ~/.config/hypr/hypridle.conf"
alias hl="nvim ~/.config/hypr/hyprlock.conf"
alias hs="nvim ~/.config/hypr/hyprsunset.conf"
alias nvs="nvim /mnt/Disk2/dotfiles/hyprland/setup.sh"

# mangowm 
alias mc="nvim ~/.config/mango/config.conf"
alias ms="nvim ~/.config/swaylock/wlsunset.sh"
alias mi="nvim ~/.config/swaylock/swayidle.sh"
alias ml="nvim ~/.config/swaylock/config"
alias nvs="nvim /mnt/Disk2/dotfiles/mango/setup.sh"


# COMMANDS
alias update-grub="sudo grub-mkconfig -o /boot/grub/grub.cfg"
alias update-mirrors="sudo cp /etc/pacman.d/mirrorlist /tmp/ && sudo reflector --latest 20 --sort rate --download-timeout 30 --save /etc/pacman.d/mirrorlist --verbose"
alias ipaddr="ip -c a"
alias rn="sudo systemctl restart NetworkManager"

# yayf menu 
alias yayf="yay -Slq | fzf --multi \
  --preview 'yay -Si {1}' \
  --preview-label 'alt-p: toggle description, alt-B/b: toggle PKGBUILD, alt-j/k: scroll, tab: multi-select' \
  --preview-label-pos 'bottom' \
  --preview-window 'down:65%:wrap' \
  --bind 'alt-p:toggle-preview' \
  --bind 'alt-d:preview-half-page-down,alt-u:preview-half-page-up' \
  --bind 'alt-k:preview-up,alt-j:preview-down' \
  --bind 'alt-b:change-preview:yay -Gp {1} | tail -n +5' \
  --bind 'alt-B:change-preview:yay -Si {1}' \
  --color 'pointer:green,marker:green' \
  | xargs -ro yay -S"

# mount/umount android
alias mount-android="simple-mtpfs --device 1 /mnt/android"
alias umount-android="fusermount -u /mnt/android"

# CD TO PLACES
alias cdd="cd ~/Dokumenty"
alias cdh="cd ~/Hudba"
alias cdD="cd /mnt/Disk2"
alias cds="cd ~/Stažené"
alias cdu="cd /run/media/tom"
alias cdf="cd ~/Filmy2"

